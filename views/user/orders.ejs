<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/fonts.css">
    <!-- <link rel="stylesheet" href="/styles/style.css"> -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@40,200,0,0" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,200,0,0" />
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet"/>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
</head>

<body class="bg-stone-300 relative p-4 pb-0 overflow-y-auto h-full">
    <div class="mb-4 flex items-center justify-between">
        <div class="flex items-center gap-2">
        <a href="/home"><i class="ri-arrow-left-s-line text-xl  bg-stone-900 p-1 rounded-lg text-stone-100" ></i></a>
      </div>
        <div class="flex h-8 items-end gap-0 relative">
        <h1 class="text-stone-900 tracking-tighter">COUNTER</h1>
        <img src="/images/shopping-bag.png" class="h-full">
        </div>
      </div>  
      <% if(prices.length>0) {%>
      <div class="flex flex-col gap-5 py-5 pb-32">
        <% prices.forEach(function(price,index){ %>
          <div class="w-full bg-stone-300 shadow-2xl px-4 rounded-xl flex justify-between text-stone-900 p-2">
            <div>
              <h1>Total Amount :</h1>
              <h1>Total Products :</h1>
              <h1>Date of Order:</h1>
            </div>
            <div>
              <h2><%= prices[index] %></h2>
              <h2><%= products[index] %></h2>
              <h2><%= dates[index] %></h2>
            </div>
          </div>
      <%  }) %>
      <% } else{ %>
        <div class="w-full pt-28 text-center flex justify-center items-center flex-col gap-2 text-stone-500 text-lg">
          <h1>Why to be normal ?<br>
            when you can be the best
          </h1>
          <a href="/home" class="px-4 py-1 w-32 rounded-lg text-stone-100 bg-stone-900 mt-10 ">Let's go </a>
        </div>
      <%  } %>
      

    </div>
    <%- include('../partials/navbar') %>
</body>
</html>